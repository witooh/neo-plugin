{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "bash": "printf '%s\\n' '{\"additionalContext\":\"neo loaded. Before acting on any task, load and follow the using-neo single entry point. Let it route focused work or the lifecycle, then follow the selected method skills before implementation.\"}'",
        "powershell": "Write-Output '{\"additionalContext\":\"neo loaded. Before acting on any task, load and follow the using-neo single entry point. Let it route focused work or the lifecycle, then follow the selected method skills before implementation.\"}'",
        "timeoutSec": 5
      }
    ]
  }
}
