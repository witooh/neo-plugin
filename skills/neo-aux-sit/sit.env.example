# Copy to a path outside the repo (e.g. shell rc or ~/.config/neo/sit.env) and fill in.
# Never commit real profile names if you treat them as private — this file is only a template.
#
#   set -a && source /path/to/sit.env && set +a

NEO_AUX_AWS_PROFILE=
# Optional (core skill reads this name):
# NEO_CORE_AWS_PROFILE=
