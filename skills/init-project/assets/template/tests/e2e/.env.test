# Local default. CI overrides API_BASE_URL with the compose service name
# (http://neo-service:8080) because the runner joins the compose network.
# dotenv does not override variables already present in the environment, so the
# CI value wins.
API_BASE_URL=http://localhost:8080
