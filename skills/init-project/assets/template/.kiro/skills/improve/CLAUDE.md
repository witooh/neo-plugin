This skill is Kiro-only. It is part of the neo-power Kiro Agent Skill bundle and installs into `~/.kiro/skills/improve/` via `setup.sh`.

Do NOT add multi-CLI fallbacks (Claude Code `AskUserQuestion`, Copilot `ask_user`, etc.) or platform-detection branches. All user interactions use Kiro's plain-text chat with numbered options.
