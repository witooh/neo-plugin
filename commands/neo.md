---
description: "Route a software-development task to the neo-team specialist agents via impact-based analysis — supports single-role calls (สร้าง AC, gen test cases, review PR, fix bug), multi-role tasks (เพิ่ม endpoint, refactor), and GitLab MR create/review (สร้าง MR, review MR)"
---

Invoke the `neo-team` skill using the `Skill` tool to orchestrate the full specialist agent team for the user's request below.

When invoking, pass the full text of `$ARGUMENTS` as the user's task description. If `$ARGUMENTS` is empty, ask the user what they want the team to do before invoking the skill.

User request:

$ARGUMENTS
