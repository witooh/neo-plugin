{
  "id": "code-review",
  "skills": ["code-review"],
  "fixture": "go-service",
  "setup": "python3 - <<'PY'\nimport pathlib\np = pathlib.Path('internal/account/handler.go')\ns = p.read_text()\ns = s.replace('import (\\n\\t\"encoding/json\"', 'import (\\n\\t\"encoding/json\"\\n\\t\"log\"')\ns = s.replace('\\tacc, err := h.repo.Find(id)', '\\tlog.Printf(\"lookup account id=%s token=%s\", id, r.Header.Get(\"Authorization\"))\\n\\tacc, err := h.repo.Find(id)')\np.write_text(s)\nPY\ngit -c user.email=eval@neo -c user.name=eval commit -aqm 'add lookup logging'",
  "prompt": "review การเปลี่ยนแปลงตั้งแต่ HEAD~1",
  "expect": {
    "skillsLoaded": ["code-review"],
    "filesNotWritten": ["^internal/.*\\.go$"],
    "ranCommand": ["git (diff|log|show)"],
    "sandbox": true,
    "noGitWrites": true
  }
}
