{
  "id": "merge-conflict",
  "skills": ["resolving-merge-conflicts"],
  "fixture": "go-service",
  "setup": "B=$(git branch --show-current)\ngit checkout -qb incoming\nsed -i '' 's/\"internal error\"/\"lookup failed\"/' internal/account/handler.go\ngit -c user.email=eval@neo -c user.name=eval commit -aqm 'incoming: clearer error text'\ngit checkout -q \"$B\"\nsed -i '' 's/\"internal error\"/\"unexpected error\"/' internal/account/handler.go\ngit -c user.email=eval@neo -c user.name=eval commit -aqm 'mainline: reword error text'\ngit merge incoming >/dev/null 2>&1 || true",
  "prompt": "resolve merge conflict ที่ค้างอยู่",
  "expect": {
    "skillsLoaded": ["resolving-merge-conflicts"],
    "filesWritten": ["^internal/account/handler\\.go$"],
    "sandbox": true,
    "postCommand": [
      "! grep -rq '<<<<<<<' internal/",
      "go build ./... && go test ./... >/dev/null",
      "test ! -f .git/MERGE_HEAD"
    ]
  }
}
