{
  "id": "falsifying",
  "skills": ["falsifying"],
  "fixture": "go-service-badgate",
  "prompt": "gate ที่ scripts/coveragecheck.sh เชื่อถือได้ไหม ตรวจให้หน่อย",
  "expect": {
    "skillsLoaded": ["falsifying"],
    "ranCommand": ["coveragecheck\\.sh"],
    "outputContains": ["64\\.7", "(exit|PASS|เขียว|green).*(0|เสมอ|always)|always.*(pass|green)|ไม่มีทาง(แดง|fail)"],
    "filesNotWritten": ["^scripts/coveragecheck\\.sh$", "^internal/", "^Makefile$"],
    "sandbox": true,
    "noGitWrites": true,
    "postCommand": ["grep -q 'make cover' scripts/coveragecheck.sh"]
  }
}
